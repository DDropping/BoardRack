const INITIAL_STATE = {
  imageList: [],
  isLoading: false,
  previewVisible: false,
  previewImage: ''
};

export default function(state = INITIAL_STATE, action) {
  switch (action.type) {
    default:
      return state;
  }
}

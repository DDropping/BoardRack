import React from 'react';
import { connect } from 'react-redux';

const Home = props => {
  return (
    <div>
      <div>Home Page</div>
    </div>
  );
};

export default connect(
  null,
  null
)(Home);

import React from 'react';

const PostModalFooter = () => {
  return (
    <div style={{ width: '100%', height: 'auto' }}>
      <img
        style={{ maxWidth: '100%', height: 'auto' }}
        src={process.env.PUBLIC_URL + '/images/br_board_outlines_blue.png'}
      ></img>
    </div>
  );
};

export default PostModalFooter;
